# Objective Session Engine

Objectives sit above workspace surfaces. The active objective persists until the user explicitly switches, closes, completes, or starts a new task.

Voice routing decides whether an utterance should continue, refine, add context to, create, switch, complete, or request approval for an objective. The existing workspace patch pipeline still owns rendering changes.
