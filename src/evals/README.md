# SeemlessBench

SeemlessBench is the deterministic workflow eval suite for Adaptive Surface.

It checks objective routing, surface persistence, supporting surface creation, approval safety, context refresh correctness, and unknown fallback behavior across email, Calendar, Notes, Reminders, files, multi-surface, persistence, and approval tasks.
